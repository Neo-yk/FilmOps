# Core
torch==2.5.1
torchvision==0.20.1
numpy>=1.21.0
opencv-python>=4.5.0
Pillow>=9.0.0

# Video processing
ffmpeg-python>=0.2.0
decord>=0.6.0

# Transformers (for MLLM operators)
transformers==4.49.0
einops>=0.4.0

# Utilities
tqdm>=4.60.0

# Required at runtime by trust_remote_code model code (InternVL) and the
# DINOv2/DINOv3 hubconf backbones, even though FilmOps does not import them
# directly.
timm>=0.9.0
peft>=0.4.0